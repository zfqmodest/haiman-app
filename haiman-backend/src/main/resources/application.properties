# Server Configuration
server.port=${PORT:8081}
server.address=0.0.0.0
server.forward-headers-strategy=native

# MySQL Database Configuration
spring.datasource.url=jdbc:mysql://127.0.0.1:3306/haiman?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=13938286218ZFQ
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA Configuration
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false

# Logging
logging.level.com.haiman.backend=INFO
logging.level.org.springframework.security=INFO
logging.level.org.springframework.web=INFO
logging.file.name=haiman.log

# CORS Configuration
spring.web.cors.allowed-origins=http://localhost:5175,http://localhost:5176,http://admin.zfqqjb.top:5175,http://admin.zfqqjb.top:5176,http://admin.zfqqjb.top
spring.web.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
spring.web.cors.allowed-headers=Authorization,Content-Type,X-Requested-With,Accept,Origin,Access-Control-Request-Method,Access-Control-Request-Headers
spring.web.cors.exposed-headers=Authorization,Content-Type
spring.web.cors.allow-credentials=true
spring.web.cors.max-age=3600

# Server tomcat settings
server.tomcat.threads.max=200
server.tomcat.connection-timeout=5s
server.tomcat.max-swallow-size=2MB
server.tomcat.max-http-form-post-size=2MB

# Spring MVC settings
spring.mvc.pathmatch.matching-strategy=ant_path_matcher